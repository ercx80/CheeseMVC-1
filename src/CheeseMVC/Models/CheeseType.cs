using System;
namespace CheeseMVC.Models
{
    public enum CheeseType //an enum represents a class with finite(fixed) amount of values. They are represented by int data type

    {
        Hard, Soft, Fake
    
       

    }
}
